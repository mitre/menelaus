#not to specify the dependencies of your package, but the packages needed to work
# on developing your package.
jupyter
pandas
numpy
sklearn
scipy
matplotlib #matplotlib and plotly libraries are only strictly necessary for the kdqTree visualization stuff
plotly
black
pylint
pytest
coverage
sphinx #sphinx packages are only useful if the user is going to update documentation
sphinx-rtd-theme
sphinx-autoapi
-e . #install current (molten) directory in editable mode